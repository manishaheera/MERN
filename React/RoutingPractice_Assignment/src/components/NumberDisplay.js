import React from 'react';


const NumberDisplay = (props) => {

    const {id} = props
    // if (isNaN(word)) { ??
    return(
        <div>
        {id} 
        </div>
    )
}

export default NumberDisplay;

